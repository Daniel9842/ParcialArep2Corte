package edu.escuelaing.arep.parcial.service;

public class CalculatorFuncService {
        public static Double expFunction(Double value){
            return Math.pow(Math.E,value);
        }

        public static Double logFunction(Double value){
            return Math.log(value);
        }
}
